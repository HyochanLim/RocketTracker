# Copy to `.env` (never commit `.env`). Used by app / agent sandbox.
# E2B sandbox (npm run agent:sandbox)
E2B_API_KEY=

# 선택만: 사용자별 last-parsed-flight-<userId>.json 대신 다른 파일을 쓸 때
# PARSED_JSON_PATH=data/storage/parsed_json/USER_ID/your-flight.json
