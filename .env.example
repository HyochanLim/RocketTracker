# Copy to `.env` (never commit `.env`). Used by app and E2B (`logic/aiAgent/e2b-chat.cjs`).
E2B_API_KEY=

# 선택만: E2B 등에서 특정 파싱 JSON을 지정할 때 (미사용이면 비워 둠)
# PARSED_JSON_PATH=data/storage/parsed_json/USER_ID/your-flight.json
